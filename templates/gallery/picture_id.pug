extends ../layout.pug
include ./mixins.pug

block title
  title Photo-Gallery

block body
    div#mainBox
      div.mainPhoto
        img.mainPicture(src= picture.link )
        p#titleName #{picture.title}
        p#sideLink #{picture.link}
        br
        p#description #{picture.description}

