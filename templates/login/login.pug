<!DOCTYPE html>
html(lang="en")
head
 meta(charset="UTF-8")
 title Login
body
  //- if first time through the page
  form(method="POST" action="/login")
    input(type="text" name="username")
    input(type="text" name="password")
    button(type="submit") Login
  a(href="/logout") Logout
  br
  a(href="/registration") Registration

